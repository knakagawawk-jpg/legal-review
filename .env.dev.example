# ============================================
# 開発環境用設定ファイル（サーバー上）
# ============================================
# 使用方法: このファイルを .env.dev としてコピーして使用
# cp .env.dev.example .env.dev

# ============================================
# 認証設定（重要！）
# ============================================
# 認証機能を有効にするかどうか（true/false）
# 開発環境では通常 false に設定（認証なしで動作）
AUTH_ENABLED=false

# Google OAuth設定（Google Cloud Consoleで取得）
# 開発環境では認証を無効にするため、設定不要（AUTH_ENABLED=falseの場合）
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# JWT用のシークレットキー（開発環境用）
SECRET_KEY=dev-secret-key-change-in-production

# ============================================
# フロントエンド設定（Next.js用）
# ============================================
# Google OAuth設定（フロントエンド用）
# 開発環境では認証を無効にするため、設定不要
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# バックエンドAPIのURL（内部通信用）
# Docker Composeでは backend-dev（コンテナ名）を使用
BACKEND_INTERNAL_URL_DEV=http://backend-dev:8000

# 講評生成のタイムアウト（ミリ秒、デフォルト: 600000 = 600秒）
REVIEW_TIMEOUT_MS=600000

# ============================================
# バックエンド設定（FastAPI用）
# ============================================
# データベースURL（開発環境用）
DATABASE_URL=sqlite:////data/dev.db

# FastAPIのベースURL（フロントエンド用）
API_BASE_URL=http://backend-dev:8000

# ============================================
# Anthropic API設定
# ============================================
# https://console.anthropic.com/settings/keys で取得
# 開発環境でもAPIキーが必要（LLM機能を使用する場合）
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# デフォルトのClaudeモデル
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# 用途別のモデル設定（オプション）
# ANTHROPIC_MODEL_REVIEW=claude-3-5-sonnet-20241022
# ANTHROPIC_MODEL_REVIEW_CHAT=claude-haiku-4-5-20251001
# ANTHROPIC_MODEL_FREE_CHAT=claude-haiku-4-5-20251001
# ANTHROPIC_MODEL_REVISIT_PROBLEMS=claude-haiku-4-5-20251001
# ANTHROPIC_MODEL_TITLE=claude-haiku-4-5-20251001

# ============================================
# Caddy設定（リバースプロキシ）
# ============================================
# ローカル開発時のみ: Basic 認証なしにする場合に指定（サーバーでは指定しない）
# CADDYFILE_DEV_PATH=./Caddyfile.dev.local

# 開発環境のドメイン（サブドメイン）
CADDY_DOMAIN_DEV=dev.juristutor-ai.com

# Caddy Basic認証設定（開発環境用・サーバーで Caddyfile.dev を使う場合）
# パスワードハッシュの生成方法:
# docker run --rm caddy:2-alpine caddy hash-password --plaintext your-password
BASIC_AUTH_USER_DEV=admin
BASIC_AUTH_PASS_HASH_DEV=$2a$10$example_hash_replace_with_actual_hash
