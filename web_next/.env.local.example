# npm run dev 用の環境変数（ローカルで Next.js 単体起動時）
# 使用方法: このファイルを .env.local にコピーして値を編集
# cp .env.local.example .env.local
#
# ※ Docker Compose で起動する場合は不要（BACKEND_INTERNAL_URL は docker-compose で設定される）

NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# npm run dev では backend ホスト名が解決できないため localhost を指定
# FastAPI をローカルで起動（例: uvicorn app.main:app --reload）している前提
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
